## 1. 代码实现

- [x] 1.1 将 `resource_tc_teo_function_rule.go` 中所有 Schema 字段的 `Description` 从英文翻译为中文，包括顶层字段和嵌套字段（`function_rule_conditions`、`rule_conditions` 等）

## 2. 验证

- [x] 2.1 确认修改后的 Go 文件格式正确，可通过 `gofmt` 检查
- [x] 2.2 确认修改不涉及任何业务逻辑变更，仅 Description 字符串值发生变化