# Tasks

## 1. Schema & Resource Definition

- [x] 1.1 Add `name` (Optional, TypeString) schema field to `ResourceTencentCloudVpcReplaceRoutesWithRoutePolicyConfig()` — used to populate the `Name` of a `Filter` in the `DescribeRouteTables` read request.
- [x] 1.2 Add `values` (Optional, TypeList of TypeString) schema field — used to populate the `Values` of the same `Filter` in the `DescribeRouteTables` read request.
- [x] 1.3 Add `need_router_info` (Optional, TypeBool) schema field — maps to `request.NeedRouterInfo` of `DescribeRouteTables`.
- [x] 1.4 Add `route_table_ids` (Optional, TypeList of TypeString) schema field — maps to `request.RouteTableIds` of `DescribeRouteTables`. Mutually exclusive with Filters (including the `route-table-id` filter derived from `d.Id()`); when set, the read path queries by `RouteTableIds` only.
- [x] 1.5 Add `limit` (Optional, TypeInt, Validate: 1–100) schema field — maps to `request.Limit` of `DescribeRouteTables`. When unset, the helper uses its internal default of 100.

## 2. Read Path — Dedicated Helper

- [x] 2.1 Add a new method `VpcService.DescribeRouteTablesForReplaceRoutesWithRoutePolicyConfig(ctx, routeTableId, filterName string, filterValues []*string, needRouterInfo *bool, routeTableIds []*string, limit int)` (or equivalent) that builds its own `DescribeRouteTablesRequest` so the widely-used `DescribeRouteTables` helper signature stays untouched.
  - When `routeTableIds` is non-empty: set `request.RouteTableIds` and do NOT add any `Filters` (cloud API forbids specifying both).
  - Otherwise: add the `route-table-id` filter from `routeTableId` (when non-empty), and when `filterName != ""` append a `Filter{Name: filterName, Values: filterValues}`.
  - Set `request.NeedRouterInfo` when `needRouterInfo != nil`.
  - Use `limit` (clamped to 100) for the pagination `Limit`; keep the existing offset-based pagination loop.
  - Wrap the API call in `tccommon.ReadRetryTimeout` retry and return `tccommon.RetryError` on failure.

## 3. Read Path — Resource Read Method

- [x] 3.1 Update `resourceTencentCloudVpcReplaceRoutesWithRoutePolicyConfigRead` to read the new schema fields (`name`, `values`, `need_router_info`, `route_table_ids`, `limit`) from `*schema.ResourceData`.
- [x] 3.2 Call the new dedicated helper (task 2.1) instead of the shared `DescribeRouteTables` helper.
- [x] 3.3 When the response is empty, first `log.Printf("[CRUD] tencentcloud_vpc_replace_routes_with_route_policy_config id=%s", d.Id())`, then `d.SetId("")` and return `nil` (preserve existing behavior, do NOT silently clear id).
- [x] 3.4 The new fields are query-input only; do NOT `d.Set(...)` them from the response (they are not resource attributes).

## 4. Create / Update Path Verification

- [x] 4.1 Confirm `route_table_id` is already wired to `request.RouteTableId` in `resourceTencentCloudVpcReplaceRoutesWithRoutePolicyConfigUpdate` (currently set from `d.Id()`). No code change needed if already correct.
- [x] 4.2 Confirm `routes` is already wired to `request.Routes` in `resourceTencentCloudVpcReplaceRoutesWithRoutePolicyConfigUpdate`. No code change needed if already correct.
- [x] 4.3 Add the new read-only query fields (`name`, `values`, `need_router_info`, `route_table_ids`, `limit`) to the `immutableArgs` check if the resource uses a CRD-only immutable-args pattern — verify whether this resource enforces immutability; if it does, treat the new fields as mutable (they only affect read) and skip them from `immutableArgs`. If the resource does NOT use the immutable-args pattern, no action needed.

## 5. Unit Tests (mock-based, gomonkey)

- [x] 5.1 Add mock-based unit tests in `resource_tc_vpc_replace_routes_with_route_policy_config_test.go` using `gomonkey` to mock the VPC client's `DescribeRouteTablesWithContext` (and `ReplaceRoutesWithRoutePolicyWithContext` where needed). Do NOT use the Terraform acceptance test suite for the new parameters.
- [x] 5.2 Test case: read with `name` + `values` filters — verify the request carries a `Filter{Name, Values}`.
- [x] 5.3 Test case: read with `route_table_ids` set — verify the request uses `RouteTableIds` and NO `Filters`.
- [x] 5.4 Test case: read with `need_router_info = false` — verify `request.NeedRouterInfo == false`.
- [x] 5.5 Test case: read with `limit = 50` — verify `request.Limit == "50"`.
- [x] 5.6 Run `go test -gcflags=all=-l ./tencentcloud/services/vpc/ -run TestVpcReplaceRoutesWithRoutePolicyConfig` and ensure all new tests pass.

## 6. Docs

- [x] 6.1 Update `tencentcloud/services/vpc/resource_tc_vpc_replace_routes_with_route_policy_config.md` to add example usage demonstrating the new optional query parameters (`name`, `values`, `need_router_info`, `route_table_ids`, `limit`). Do NOT add `Argument Reference` / `Attribute Reference` sections (auto-generated by `make doc`).
- [x] 6.2 The `website/docs/r/...` file is regenerated via `make doc` during the finalization phase — do NOT edit it directly.
